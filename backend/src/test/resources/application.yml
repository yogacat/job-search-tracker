spring:
  datasource:
    url: jdbc:h2:mem:jobsearch_tracker;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  test:
    database:
      replace: none
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
